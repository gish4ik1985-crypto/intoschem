# Аудит проекта — 2026-08-31

Что найдено машинной проверкой веб-сборки (`web/`). Каждый пункт — задача на
правку. Отмечать сделанное прямо здесь.

Методика: node-харнесс грузит всю веб-сборку без браузера (заглушки DOM),
дальше по ней гоняются проверки. Скрипты лежат в `web/tests/`.

---

## Что проверено и оказалось в порядке

- **Решатель верен во всех режимах.** 16 аналитических проверок: делитель,
  последовательно/параллельно, разомкнутый ключ, плавающий узел (gmin), заряд RC
  и нарастание RL против аналитики, диод прямо и обратно с проверкой
  самосогласованности, транзистор в активном режиме (β = 100,0 ровно) и в
  насыщении (Vc = 0,038 В), развёртка по базе 1 кОм … 1 МОм. **Ни один узел
  нигде не выходит за пределы питания.** Баг транзистора в насыщении закрыт по
  существу, а не по симптому. Константы GDScript и JS совпадают.
- **Все уровни проходимы.** Перебор всех достижимых состояний игрока (для
  14-го — 2197 комбинаций гнёзд): победа достижима, вырождения матрицы нет ни в
  одном состоянии, узлы нигде не выходят за `vmax`. Уровень 10 проверен
  последовательностью «заряд → спуск»: пик 1,48–1,50 А при требуемых 1,2 А.
- **Свет на карте с запасом.** Все переходы: минимальный зачтённый score против
  расстояния до соседа. Худший — `divider → heater`: нужно 0,41, минимум 0,67.
- **Производительность.** 5–30 мкс на шаг решателя, 0,05–0,24 мс на кадр при
  бюджете 16,7.
- **Кодекс.** 21 статья, ни одной ссылки уровня без статьи, ни одной
  статьи-сироты, ни одного термина из внутренностей в игровом тексте.
- **`concept.html` побайтово соответствует `concept.md`** (пересборка не меняет
  хеш).
- **Схема уровня.** Все обязательные поля везде, подсказки везде, самописец
  везде.

---

## A. Дефекты картинки [ЗАКРЫТО]

Ни один не виден в логе и не ловится решателем: провода в фиксированных уровнях
декоративны, топологию задаёт `build()`.

### [x] A1. Уровень 4 «Гирлянда» — провод цепочки идёт сквозь пропущенную лампу

Когда лампа посреди ряда переключена в «параллельно», звено цепочки идёт
напрямую от предыдущей последовательной лампы к следующей — то есть **сквозь
корпус** пропущенной. Её собственные отводы на VCC и GND кончаются ровно на
этом проводе, и `wireJunctions` честно ставит там паяный пятак: картинка
утверждает, что параллельная лампа сидит в цепочке.

Ловится в 20 состояниях из 32. Уровень, весь смысл которого —
«последовательно ≠ параллельно».

**Как чинить:** звено цепочки между двумя последовательными лампами, между
которыми стоят параллельные, разводить в обход — через свободную полосу
(например `y = ряд ± 34`), а не по прямой на высоте ряда.

### [x] A2. Уровень 13 «Датчик» — земляная шина сквозь транзистор

Шина `y = 106` идёт сквозь корпус транзистора (`y 46..122`), в 12 px над всеми
тремя его ножками. Читается как «все три вывода на земле».

Там же: в режиме `amp` шина торчит до `x = 270`, хотя последний отвод на
`x = 174` — конец в пустоте.

**Как чинить:** опустить шину ниже корпуса транзистора и обрезать её по
фактическому крайнему отводу (приём из уровня 4: `railEnd(taps)`).

### [x] A3. Уровень 16 «Просадка» — шина VCC сквозь кнопку и стартер

Шина `y = -20` проходит сквозь корпуса `BTN` (`y -34..18`) и `STARTER`
(`y -47..31`). Отдельно: провод `ST` начинается в точке `(-110,-8)` — это 8,5 px
**внутри** корпуса кнопки, а не на её выводе `(-104,-2)`. Тот же дефект, который
на этом уровне уже правили один раз.

### [x] A4. Уровень 10 «Вспышка» — провод сквозь лампу

Провод `CAP` от накопителя к спуску идёт горизонтально на `y = 46` сквозь корпус
лампы-вспышки (`x 56..124`, `y 26..78`), в 6 px над её выводами.

### [x] A5. Уровень 12 «Маячок» — вывод реле в никуда

У реле вывод `comB (8,68)` не подключён ничем. Контакт замыкает `CAP` на `FL`, и
сторона `CAP` нарисована не подходящей ни к чему: разрядный контур на картинке
разомкнут.

### [x] A6. Уровень 7 «Подогрев» — знак земли за краем платы

`GNDS` в `(-40,118)`, габарит 44×34, нижний край платы `y = 132`: вылезает на
3 px. Ровно то, что уже поймали и подвинули на уровне 10 (со 122 на 104), но на
7-м не тронули.

---

## B. Механика и тексты [ЗАКРЫТО]

### [x] B1. Уровень 9 — светодиод сгорает раньше, чем читается бриф

Замерено: `tau = 20` даёт сгорание на **8,3 с** после входа. Бриф — 27 слов
≈ **10,8 с** спокойного чтения. Правило, записанное в `CLAUDE.md` после двух
заходов на этот же уровень, всё ещё нарушено.

**Как чинить:** `tau ≈ 45–50` даёт ~20 с — заведомо с запасом.

### [x] B2. Самописец уезжает вместе с платой при зуме и панораме

`drawScope` вызывается внутри трансформированного контекста (`level.js`,
`render()`, до `ctx.restore()`). Пока масштаб был фиксированным, это ничего не
значило; после появления зума при `zoom = 2.5` ленту уносит за экран.

**Как чинить:** вынести `drawScope` за `ctx.restore()` и считать `SCOPE_RECT` в
экранных координатах (как `flashMsg`), а не в координатах платы.

### [x] B3. Уровень 17 — подписи деталей теряют номер после перезахода

`placeAt` дописывает номер в `silk` (`R` → `R3`), а `persist()` поле `silk` не
сохраняет; `make(kind)` при загрузке возвращает базовое. После выхода и возврата
все резисторы подписаны просто «R», все светодиоды — «VD».

**Как чинить:** добавить `silk` в список сохраняемых полей свободной сборки.

---

## C. Структурное

### C1. У боевого решателя не было ни одного теста

Тесты решателя есть у Godot-сборки (`tests/test_runner.gd`), которая не
запускается; у той версии, что крутится в игре (`web/js/circuit.js`), их не было
вообще. Порт «штамп-в-штамп» ничем не защищён.

**Закрыто:** `web/tests/solver.test.js`.

### C2. Проверки из CLAUDE.md надо гонять скриптом, а не глазами

Все шесть дефектов раздела A нашлись за минуту машинной проверкой по признакам,
которые в `CLAUDE.md` уже описаны словами — и все шесть на уровнях, где эти же
проверки уже делали вручную.

**Закрыто:** `web/tests/layout.test.js` (пять признаков разводки + наезды
деталей + края платы + шелкография + провод сквозь корпус, по всем состояниям
переключателей) и `web/tests/levels.test.js` (проходимость, пределы узлов,
свет на карте, целостность кодекса).

### C3. Две сборки: Godot и HTML

Godot остался на трёх уровнях и старом визуале (`exe` — rev 0.1), вся игра живёт
в `web/`. Концепт сам называет это единственным открытым вопросом.

**Мнение:** закрывать в пользу `web`. Единственное, что `core/*.gd` реально
давал, — тесты решателя, и они теперь есть у веб-сборки.

---

## Порядок работ

1. ~~`web/tests/` — солвер и разводка~~ — сделано.
2. ~~A1–A6 — разводка шести старых уровней~~ — сделано.
3. ~~B1–B3~~ — сделано.
4. Решение по C3 — не принято (мнение прежнее: закрывать в пользу `web`).

`web/tests/known-issues.json` теперь содержит только то, что вне первоначального
аудита и оставлено осознанно: не полностью подключённое гнездо `SUPL.b` на 9-м
уровне (отдельный, не описанный здесь дефект) и намеренно не подключённые
`RB.a`/`RB.b` на 13-м (гнездо и транзистор в режиме «напрямую» физически на
плате, но в цепь не входят — это и есть смысл уровня, см. правило в `CLAUDE.md`
про исключение для намеренно неподключённых деталей).

---

## Состояние после работ по тайне и тридцати новым узлам

`node web/tests/run.js` — 338 проверок проходит, 0 новых замечаний, 33
известных из этого файла. Тридцать новых узлов (секторы Б, В, Г, Д) чисты по
всем семи признакам разводки, проходимы, дают задуманный ответ (проверено
`web/tests/_wins.js`), не выводят узлы за пределы питания и укладываются в свет
на карте. Подробности работ — в `WORK.md`.

---

## Повторный обход разводки (2026-09-19)

Запрос: провода не доходят до выводов, пересечения выглядят неаккуратно.
Считал не глазами, а скриптом: точный зазор до ближайшего вывода или чужого
провода (без допуска 20 px из `layout.test.js`) и пересечения разных сетей.

**Исправлено (два настоящих зазора, оба на прямой линии той же сети):**
- `beacon` (`l12_beacon.js`): GND-провод катушки кончался на y=-38, а вывод
  `SUP.a` на y=-56 — зазор 18 px, на экране виден. Конец сдвинут в -56.
- `sag` (`l16_sag.js`): VCC-провод не доходил 10 px до `LAMP.a`. Конец
  сдвинут с x=-64 на x=-74.

**Не тронуто, и это осознанно:**
- `flash` (`l10_flash.js`): CAP-провод кончается в (-32,46), а не в выводе
  кнопки (-40,52). Первая попытка «дотянуть» до вывода дала ложный узел —
  точка легла на FL-провод, идущий через (-40,52). Откатил. Зазор 6–8 px
  здесь защита от ложного соединения, а не небрежность.
- `sensor` (6 px до `SUP.a`) — прячется под корпусом блока питания.
- Пересечения разных сетей есть в 7 уровнях из 79 (`panel`, `switchboard`,
  `flash`, `pump`, `beacon`, `saturation`, `sag`), по 1–2 на уровень. В
  `panel` разобрал геометрию: выводы батареи стоят рядом, VCC уходит далеко
  вправо, GND заходит с другой стороны — пересечения не избежать без
  большого обхода. Такие пересечения проект разрешает (см. `CLAUDE.md`).

`node web/tests/run.js` — 809 прошло, 0 новых, 3 известных.
